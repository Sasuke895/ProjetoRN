import { StyleSheet } from 'react-native';

const estiloAlunos = StyleSheet.create({
    container: {
        flex: 1,
    },
    itemFoto: {
        height: 300,
    },
});

export default estiloAlunos;